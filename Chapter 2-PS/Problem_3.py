# Check the type of variable assigned using input () function.
a = input("Enter the value of a: ")
print(type(a))

# It will give always str because the input give always str no matter the circumstances.

# Type conversion is a different thing.