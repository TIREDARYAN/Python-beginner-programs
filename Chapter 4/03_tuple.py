# a = (1,2,3,4,5) # This is a tuple.
# a = () # This is a empty tuple.
# a = (1,) # This is a single object tuple else python thinks its an integer class.
a = (1, 2, False, "Aryan")
# a[0] = "Singh" # Tuple can't be mutable its immutable.
print(type(a)) 