friends = ["Apple", "Orange", 5, 340.25, False, "Aryan"]
print(friends)
friends.append("Ayush") # For adding at the end of the list.
print(friends)

l1 = [1,45,55,3,5,7,2,99]
# l1.sort() # This will give all the numbers in the list in ascending order.
# l1.reverse() # It will make list to start from last to start.
# l1.insert(3, 22) # It will insert in between the list first where you want to place and then object.
# l1.pop(2) # It will delete the value of index 2 =>55 in the list.
# print(l1.pop(2)) # It will give only the pop value which is deleted answer is 55 here in this case.
l1.remove(55) # It will remove the given string from the list.
print(l1)